package model;

/** This represents a Treasure interface which is used by the model and the driver for
 * its enums. It is a facade to hide the many other functionalities of the TreasureImpl that the
 * user has no need of interacting with. This interface also allows for us to modify the
 * underlying functionality and methods of the implementation without breaking any other aspect
 * of the code.
 */
public interface Treasure {

}
