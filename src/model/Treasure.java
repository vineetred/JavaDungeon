package model;

/** This represents a Treasure interface which is used by the model and the driver for
 * its enums.
 */
public interface Treasure {

  /** Returns the treasure enum that is contained within this
   * treasure object
   *
   * @return Treasure enum
   */
  Treasure getTreasure();

}
